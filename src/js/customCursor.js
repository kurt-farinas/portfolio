/* ========================================
   CUSTOM CLEAN CURSOR (Hardware Accelerated)
   Pure CSS zero-latency cursor
   ======================================== */

export function initCustomCursor() {
  // CSS hardware cursor handles movement with 0ms latency on GPU
}
