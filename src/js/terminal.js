/* ========================================
   TERMINAL  |  Interactive Shell (Disabled)
   ======================================== */

window.executeChip = function(cmd) {
  // No-op
};

export function initTerminal() {
  // No-op: Interactive terminal shell removed from hero section per user request
}
